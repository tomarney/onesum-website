---
export interface Props {
	text?: string;
	href?: string;
	classes?: string;
	darkOnLight?: boolean;
}
const { text = "Show me →", href = "/examples", classes = "", darkOnLight = false} = Astro.props;

const colors = darkOnLight ? "text-indigo-900 border-indigo-900 hover:bg-indigo-900/10" : "text-white border-white hover:bg-white/10"
---

<a href={href} class={`inline-block tracking-wide w-60 py-4 rounded-md text-center text-lg transition-colors border ${colors} ${classes}`}>
		{ text }
</a>